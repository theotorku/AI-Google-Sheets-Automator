OPENAI_API_KEY=sk-...
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-for-admin-tasks
# Or just use the anon key if not doing admin stuff, but python client usually needs robust access if verifying tokens universally